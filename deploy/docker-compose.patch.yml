# Referensi: blok wa-blas untuk /opt/docker-server-rsi/docker-compose.yml.
# SUDAH TERPASANG di server (wa-blas berjalan). Jangan dijeda/diubah.
# rest-api TIDAK membutuhkan service baru — kodenya dibaca via volume www:/var/www
# oleh Apache (80/443) + php74-fpm (9000) yang sudah ada.
#
# Catatan penting: wa-blas bind 127.0.0.1:3001 (lokal saja, tidak terekspos internet).
# rest-api mengakses via http://wa-blas:3001 di network webserver_webnet.

#   wa-blas:
#     build: ./www/pendaftaran-online/wa-blas
#     container_name: wa-blas
#     restart: unless-stopped
#     ports:
#       - "127.0.0.1:3001:3001"
#     environment:
#       - PUPPETEER_EXECUTABLE_PATH=/usr/bin/chromium
#       - TZ=Asia/Jakarta
#     volumes:
#       - ./www/pendaftaran-online/wa-blas/.wwebjs_auth:/app/.wwebjs_auth
#       - ./www/pendaftaran-online/wa-blas/.wwebjs_cache:/app/.wwebjs_cache
#     env_file:
#       - ./www/pendaftaran-online/wa-blas/.env
#     networks:
#       - webnet